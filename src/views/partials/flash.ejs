<% if (success && success.length) { %>
    <div class="alert alert-success rb-flash" role="alert">
        <%= success %>
    </div>
<% } %>

<% if (error && error.length) { %>
    <div class="alert alert-danger rb-flash" role="alert">
        <%= error %>
    </div>
<% } %>